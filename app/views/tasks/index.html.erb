<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タスク名</th>
    <th>詳細</th>
    <th>期限日</th>
  </tr>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.name %></td>
      <td><%= task.details %></td>
      <td><%= task.due_date %></td>
    </tr>
  <% end %>
</table>